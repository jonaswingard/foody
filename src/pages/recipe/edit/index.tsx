import Base from "@/components/Base";
import RecipeForm from "@/components/Recipe/EditRecipe";

const Recipe = () => {
  return (
    <Base>
      <RecipeForm />
    </Base>
  );
};

export default Recipe;
